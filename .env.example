# Server port (default: 3001)
PORT=3001

# Optional: enables the Gemini tab/route (server/src/core/geminiClient.ts,
# server/src/routes/gemini.ts). Leave blank to run fully offline — every
# other feature works without it.
GEMINI_API_KEY=
