# Copy this file to server/.env and fill in your values.
# The server loads this automatically on startup (dotenv).

# Server port (default: 3001)
PORT=3001

# Optional: enables the Gemini tab/route.
# Leave blank to run fully offline — every other feature works without it.
GEMINI_API_KEY=
